<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ucc.ucc_saam">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher_saam"
            android:label="SAAM"
            android:roundIcon="@mipmap/ic_launcher_saam_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ValidarRetoActivity">
        </activity>
        <activity android:name=".AceptarRetoActivity">
        </activity>
        <activity android:name=".RetoEspecificoActivity">
        </activity>
        <activity android:name=".ConfiguracionActivity">
        </activity>
        <activity android:name=".PerfilActivity">
        </activity>
        <activity android:name=".AlbumActivity">
        </activity>
        <activity android:name=".RetosActivity">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>